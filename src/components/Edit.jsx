export const Edit = () => {
    return (
        <article className="button-wrapper">
            <button onClick={() => setIsSubmitted(false)}>
                Edit answers
            </button>
        </article>
    );
};